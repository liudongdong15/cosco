# Code Review

## Scope

- Mode: current workspace changes
- Branch or PR: `main`
- Base: `main`
- Output file: `docs/reviews/code-review-20260524-1245.md`
- Included scope: current implementation under `scripts/`, generated metric CSVs under `data/`, and HTML report generation behavior under `reports/`
- Excluded scope: annual report PDF source content audit and manual verification of every extracted financial number
- Parallel review coverage: none

## Findings

No material findings.

The previous review issue is fixed: passing the raw candidate metrics file to `scripts/build_value_line.py` now exits with a clear actionable error instead of throwing a Python traceback. Reviewed metrics still generate the HTML report successfully.

## Verification

- Ran Python syntax compilation for:
  - `scripts/build_corpus.py`
  - `scripts/extract_metrics.py`
  - `scripts/validate_metrics.py`
  - `scripts/build_value_line.py`
- Ran report generation with raw candidate metrics:
  - `python3 scripts/build_value_line.py --metrics data/value_line_metrics.csv --output /private/tmp/cosco_review3_raw_metrics_report.html`
  - Result: exits non-zero with a clear message telling the user to run `validate_metrics.py` first.
- Ran report generation with reviewed metrics:
  - `python3 scripts/build_value_line.py --metrics data/value_line_metrics_reviewed.csv --output /private/tmp/cosco_review3_reviewed_report.html`
  - Result: succeeds.
- Ran strict validation on the candidate metrics file:
  - `python3 scripts/validate_metrics.py --strict --input data/value_line_metrics.csv --output /private/tmp/cosco_review3_metrics.csv`
  - Result: exits non-zero and lists unreviewed candidate values, as intended.
- Inspected the reviewed HTML output for empty-data handling and CCFI wording:
  - Empty chart/table sections render as `暂无数据` or `暂无分部收入数据`.
  - Dividend rows with missing values render `--` and `待复核`.
  - The previous unsupported `高度正相关` wording is not present.

## Open Questions

- None.

## Residual Risk

- There are no automated unit tests in the reviewed change set, so this review relies on CLI-level regression checks and source inspection.
- The workspace still contains generated SQLite sidecar files such as `*.db-wal` and `*.db-shm`; before committing, confirm whether generated runtime artifacts should be ignored or committed.
- This review did not validate the factual accuracy of every extracted metric against the source annual report PDFs.
